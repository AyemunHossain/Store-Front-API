export interface CarBackLight {
    _id: string;
    back_lighting_type: string;
    adjustable_backlight: string;
    adjustableBacklight: boolean;
    createdAt: string;
    updatedAt: string;
    success?: boolean;
}